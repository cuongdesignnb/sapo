version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kiotviet-app
    ports:
      - "8080:8080"
    volumes:
      - ./storage:/var/www/storage
    env_file:
      - .env.docker
    restart: unless-stopped
