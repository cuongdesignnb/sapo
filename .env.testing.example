# .env.testing — Dùng cho automated test trên MySQL Docker
# Copy sang .env.testing trước khi chạy test:
#   Copy-Item .env.testing.example .env.testing
#
# Yêu cầu: docker compose -f docker-compose.testing.yml up -d

APP_NAME=KiotVietClone
APP_ENV=testing
APP_KEY=base64:qUGGmND6tGLuPD2LSLQbo4V8K8q/bFdhALKRVUVD8No=
APP_DEBUG=true
APP_TIMEZONE=Asia/Ho_Chi_Minh
APP_URL=http://localhost

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3319
DB_DATABASE=sales_test
DB_USERNAME=test_user
DB_PASSWORD=test_password

CACHE_STORE=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync
MAIL_MAILER=array

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
